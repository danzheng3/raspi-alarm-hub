#include <stdio.h>
#include <stdlib.h>
#include <iostream>

class audioManager {
    public:
        audioManager(); //need to initialize sdCard here. 
        // may need to add parameters, plus duplicate depending on when sd slot is added.
        ~audioManager();


    private:

};